<% if flash[:notice] %>
  <p style="color: limegreen;"><%= flash[:notice] %></p><p>
<% end %><h1>Editing Book</h1>
<!-- form_with部分 -->
  <% if @book.errors.any? %>
  <p style="color: red;"><%= @book.errors.count %>errors prohibited this book from being saved:</br>
  <p style="color: red;"><% @book.errors.full_messages.each do |message| %>
    <%= message %>
    </br>
  <% end %>
  <% end %>
<%= form_with model:@book, local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4></h4>
  <%= f.text_area :body %></br>
  <%= f.submit 'Update Book' %></br>

  <%= link_to "show", book_path(@book.id) %> | <%= link_to "back", books_path %>
<% end %>